Solaris
To build libesedb on Solaris make sure that /usr/ccs/bin and /usr/swf/bin are defined in the PATH environment variable


Also see:
* README.dll
* README.macosx
* README.mingw
* README.static

